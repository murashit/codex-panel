## Changes

- Rebuilt the main Codex Panel runtime UI on React-backed components for more consistent rendering and future UI work.
- Polished chat scrolling, toolbar state, thread list spacing, rename inputs, and pending Plan mode prompts.
- Strengthened internal UI state and DOM ownership checks to reduce regressions around live updates and multiple panels.
