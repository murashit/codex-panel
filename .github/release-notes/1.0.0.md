Codex Panel 1.0 declares the current sidebar workflows stable, including persistent conversations, approvals, Plan mode, diagnostics, wikilink file mentions, archive-to-note, and selection rewrites.

## Changes

- Added `/archive` so active or selected Codex threads can be archived from the composer with the same recent-thread completion used by `/resume` and `/refer`.
