## Changes

- Keep context attachments, task progress, and diffs visible when opening forked or rolled-back conversations.
- Keep pending and running activity visible in thread lists while forks and rollbacks replace their source thread.
- Allow Selection Rewrite results to be applied after edits outside the original selection.
