## Changes

- Keep unsent composer input when creating a thread, and restore the first message if sending fails.
- Open side chats as drafts and create their threads on the first send. Returning to the source without sending leaves no new thread.
- Update compatibility for Codex CLI 0.156.0.
