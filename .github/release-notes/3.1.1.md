## Changes

- Added `/reconnect` to reconnect Codex app-server and resume the active thread from the composer.
- Added a setting to hide the chat toolbar while keeping slash-command workflows available.
- Improved composer and selection rewrite layout behavior around the Obsidian status bar and compact rewrite controls.
- Fixed Obsidian Community review warnings for selection rewrite typing and composer CSS compatibility.
